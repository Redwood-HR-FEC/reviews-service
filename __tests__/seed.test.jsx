
import { createProducts, createReviews, scrapeImage } from '../database/seed';

// jest.useFakeTimers();

describe('Seed.js Tests', () => {
  describe('Image Scraper', () => {
    test('Takes in a n of 1 to loop, check if 1 file created', () => {
      // scrapeImage(1);
      //   .then(data => {
      //     const testImage = path.join(__dirname, `database`,`data`, `avatar-0.jpg`);
      //     fs.stat(testImage, function(err, stats) {
      //       console.log(stats);
      //       expect(stats).not.toBeUndefined();
      //     });
      //   });
    });
  });
});